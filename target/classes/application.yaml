spring:
  application:
    name: learndr
  datasource:
    url: jdbc:postgresql://localhost:55432/learndrdb
    # TODO: Replace with actual database credentials
    username: postgres
    password: postgres

server:
  servlet:
    session:
      tracking-modes:
      - cookie
      cookie:
        http-only: true