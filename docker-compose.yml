version: "3.9"

services:
  db:
    image: postgres:16
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "55432:5432"

    volumes:
      - learndr_pgdata:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  learndr_pgdata:
